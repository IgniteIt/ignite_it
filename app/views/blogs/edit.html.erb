<%= form_for [@project, @blog] do |f| %>
  <%= f.label :title %>
  <%= f.text_area :title, readonly: true  %>
  <%= f.label :content %>
  <%= f.text_area :content %>
  <%= f.submit 'Submit' %>
<% end %>
