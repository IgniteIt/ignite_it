<%= simple_form_for [@project, @blog, @comment] do |f| %>
  <br>
  <%= f.text_area :comment, class: "form-control", :placeholder => "Comment" %>
  <br>
  <%= f.submit 'Submit', class: "btn btn-success" %>
<% end %>