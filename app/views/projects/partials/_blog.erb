<% if project.is_owner?(current_user) %>
  <%= link_to 'Create Blog', new_project_blog_path(project) %>
<% end %>

<% if project.blogs.any? %>
  <% project.blogs.reverse_each do |blog| %>
    <p><%= blog.title %></p>
    <p><%= blog.content %></p>
    <p><%= link_to 'Edit Blog', edit_project_blog_path(project, blog) %></p>
    <p><%= link_to 'Delete Blog', project_blog_path(project, blog), method: :delete %></p>    
    <h4>Comments</h4>
    <% if blog.comments.any? %>
      <% blog.comments.each do |comment| %>
        <p><%= comment.comment %></p>
        <%= link_to 'Edit Comment', edit_project_blog_comment_path(project, blog, comment) %>
        <%= link_to 'Delete Comment', project_blog_comment_path(project, blog, comment), method: :delete %>
      <% end %>
    <% end%>
    <p><%= link_to 'Comment', new_project_blog_comment_path(project, blog) %></p>
  <% end %>
<% end %>
